# OKF Integration for Cursor (.cursorrules)

Integrate **Open Knowledge Format (OKF)** with **Cursor** using `.cursorrules`:

```markdown
# Cursor Rules — OKF Knowledge Base Integration

1. Always consult `.okf/index.md` to understand system architecture, service definitions, and project standards.
2. Comply with all constraints defined in `.okf/rules/`.
3. When creating new API endpoints or database schemas, update `.okf/api/` and `.okf/database/`.
4. Keep concept files formatted with valid YAML frontmatter containing `type`, `title`, and `description`.
```
